{
  "hooks": {
    "post-tool-use": [
      {
        "name": "detect-project-type",
        "matcher": {
          "tool": "Read",
          "once_per_session": true
        },
        "command": "if [ -f package.json ]; then echo '📦 Node.js project detected'; elif [ -f requirements.txt ] || [ -f pyproject.toml ]; then echo '🐍 Python project detected'; elif [ -f Cargo.toml ]; then echo '🦀 Rust project detected'; elif [ -f go.mod ]; then echo '🐹 Go project detected'; fi",
        "timeout": 1000
      },
      {
        "name": "warn-before-rm",
        "matcher": {
          "tool": "Bash",
          "command_patterns": ["rm -rf", "rm -r"]
        },
        "command": "echo '⚠️  Destructive operation detected - ensure backups exist'",
        "timeout": 500
      },
      {
        "name": "check-secrets-before-write",
        "matcher": {
          "tool": "Write",
          "file_patterns": ["*.env", "*.key", "*.pem", "*config*.json"]
        },
        "command": "echo '🔐 Sensitive file detected - verify no secrets are exposed'",
        "timeout": 500
      }
    ],
    "pre-tool-use": [
      {
        "name": "git-status-check",
        "matcher": {
          "tool": "Bash",
          "command_patterns": ["git commit", "git push"]
        },
        "command": "git status --short | head -5 && echo '📊 Git status shown above'",
        "timeout": 2000
      }
    ]
  }
}
